const AppReducer = (state, action){
    switch(action.type){
        case 'ADD_CART':
            const { id } = action.payload;
            const check = ac.
        case 'REMOVE_PRODUCT':
            return{

            }
        case 'REDUCTION':
            return{

            }
        case 'INCREASE':
            return{

            }
        case 'GET_TOTAL':
            return{

            }
    }
}